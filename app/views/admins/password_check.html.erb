<%= content_for :title_tag do t('metas.titles.admin') end %>

<div class="dashboard">
  <div class="dashboard-container">
    <div class="container">
      <div class="row relative">
        <div class="col-xs-12 col-md-6 col-md-offset-3">
          <div class="form-picto">
            <i class="fa fa-key"></i>
          </div>
          <%= render "shared/field_with_button", url: admin_password_verification_path, subquery: "password", placeholder: "mot de passe", btn_text: "accéder" %>
        </div>
      </div>
    </div>
  </div>
</div>
